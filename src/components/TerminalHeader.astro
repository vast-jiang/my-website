、---
const pathname = Astro.url.pathname;
---
<header class="mb-10 relative z-40">
  <!-- 顶部装饰线 -->
  <div class="flex justify-between items-center text-xs text-dos-dim mb-1 font-mono uppercase tracking-widest">
    <span>System_Check: OK...</span>
    <span class="animate-pulse">Connected: 127.0.0.1</span>
  </div>

  <!-- 主导航栏 -->
  <div class="border-y-2 border-dos-primary bg-black/80 backdrop-blur-sm p-4 flex flex-col md:flex-row justify-between items-center relative">
    <!-- 左侧 LOGO -->
    <div class="flex flex-col items-center md:items-start mb-4 md:mb-0">
      <h1 class="text-3xl md:text-4xl font-bold tracking-tighter drop-shadow-[0_0_10px_rgba(51,255,0,0.8)]">
        DOS_SYSTEM<span class="animate-blink">_</span>
      </h1>
      <p class="text-xs text-dos-dim">KERNEL V4.0.2 // SECURE MODE</p>
    </div>
    
    <!-- 右侧 导航菜单 -->
    <nav class="flex gap-1 bg-dos-dim/20 p-1 rounded border border-dos-dim/50">
      <a href="/" class={`px-4 py-1 text-lg uppercase transition-all ${pathname === '/' ? 'bg-dos-primary text-black font-bold shadow-[0_0_10px_#33ff00]' : 'hover:bg-dos-primary/20 hover:text-white'}`}>
        [ Home中心 ]
      </a>
      <a href="#contacts" class="px-4 py-1 text-lg uppercase hover:bg-dos-primary/20 hover:text-white transition-all">
        [ 通讯录 ]
      </a>
      <!-- GitHub 已移除，改为放在下方连接池 -->
    </nav>

    <!-- 装饰性方块 (右上角) -->
    <div class="absolute top-0 right-0 w-2 h-2 bg-dos-primary"></div>
    <div class="absolute bottom-0 left-0 w-2 h-2 bg-dos-primary"></div>
  </div>
</header>